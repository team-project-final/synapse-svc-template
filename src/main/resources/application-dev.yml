spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres-dev:5432/knowledge}
    username: ${DB_USERNAME:knowledge}
    password: ${DB_PASSWORD:knowledge}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: ${REDIS_HOST:redis-dev}
      port: ${REDIS_PORT:6379}
